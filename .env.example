# flyto-i18n local environment
#
# Normal validation, build, locale sync, and coverage commands do not require
# secrets or network credentials.

# Optional. Used only by scripts/translate-with-openai.py for assisted
# translation drafts. Leave blank unless you are explicitly running that script.
OPENAI_API_KEY=
